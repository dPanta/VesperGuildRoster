package-as: VesperGuild

# Ace3 libraries are bundled locally in Libs/, so no externals needed.
# The packager will include them automatically based on VesperGuild.toc.

ignore:
  - .git
  - .gitignore
  - .pkgmeta
exit
